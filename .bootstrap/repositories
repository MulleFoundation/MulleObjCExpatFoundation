#
# building expat is difficult, because it fails to build the man file
# for which it needs a million prerequisites
# so we just brew it
# the OS X expat is too old
#
https://github.com/libexpat/libexpat/archive/R_2_2_1.tar.gz;;;tar

${MULLE_FOUNDATION_REPOSITORIES:-https://github.com/mulle-nat}/MulleObjCStandardFoundation;;${MULLE_OBJC_STANDARD_FOUNDATION_BRANCH:-release}
